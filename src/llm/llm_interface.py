class LLMInterface:
    def expand_keywords(self, keyword: str) -> list:
        # Placeholder for real LLM
        return [
            keyword,
            f"{keyword} tutorial",
            f"{keyword} pdf",
            f"{keyword} research paper"
        ]
